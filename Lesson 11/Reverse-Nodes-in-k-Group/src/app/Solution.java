package app;

class Solution {
    public ListNode reverseKGroup(ListNode head, int k) {
        if (head != null) {
            ListNode prevNode = head;
            ListNode currNode;
            ListNode nextNode;
            ListNode hListNode;

            if (head.next != null) {

            }
        }

        return head;
    }
}